package main.players;

import main.Symbol;

public class RealPlayer extends Player{

    public RealPlayer(Symbol symbol) {
        super(symbol);
    }

    public RealPlayer(String name, Symbol symbol) {
        super(name, symbol);
    }
}
